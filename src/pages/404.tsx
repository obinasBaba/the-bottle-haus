import React from 'react';
import NotFoundPage from '@/scenes/NotFoundPage';

const PageNotFound = () => {
  return (
    <div>
      <NotFoundPage />
    </div>
  );
};

export default PageNotFound;
